# NimbusCloud Assistant — backend deps
# Pin major versions known to work together; transformers must be recent enough
# for TextIteratorStreamer + the dtype= load kwarg used in the notebook.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# Model stack (matches the training notebook)
torch>=2.2
transformers>=4.44
peft>=0.11
accelerate>=0.30
sentencepiece>=0.2
